{
  "name": "@dev/webrtc",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "main": "lib",
  "files": [
    "lib/"
  ],
  "eslintConfig": {
    "env": {
      "browser": true
    }
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "scripts": {
    "build": "tsc",
    "coverage": "type-coverage && tsc --noEmit"
  },
  "dependencies": {
    "@webrtc-remote-control/react": "^0.1.3",
    "express": "^5.0.0",
    "peerjs": "^1.5.5",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/react": "^18.3.12",
    "peer": "^1.0.2"
  },
  "peerDependencies": {
    "react": "^18.2.0"
  }
}
